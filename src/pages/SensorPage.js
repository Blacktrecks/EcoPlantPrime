import React from 'react';
import { Container, Typography, Box } from '@mui/material';

const SensorPage = () => {
    return (
        <Container>
            <Typography variant="h1">Sensor Page</Typography>
            <Box>
                {/* Add your content here */}
            </Box>
        </Container>
    );
}

export default SensorPage;
